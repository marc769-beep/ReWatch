{
  "name": "rewatch-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Agente que busca Apple Watches en Vinted segun los criterios de ReWatch",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js --once",
    "watch": "node src/index.js --watch",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0"
  }
}
